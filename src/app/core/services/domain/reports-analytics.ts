import { Injectable } from '@angular/core';

/** Punto de extensión para cargar datos y construir vistas por tab (próxima iteración). */
@Injectable({ providedIn: 'root' })
export class ReportsAnalyticsService {}
